{
  "name": "decodec",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "jest": "^29.0.0",
    "jest-environment-jsdom": "^29.0.0"
  },
  "dependencies": {
    "fzstd": "^0.1.1"
  }
}
